fastapi
uvicorn[standard]
jinja2
python-multipart
insightface
onnxruntime-gpu; platform_machine == "x86_64"
onnxruntime; platform_machine != "x86_64"
ultralytics
transformers>=4.57  # Florence-2 object model (native integration; 4.57+ ships Florence2ForConditionalGeneration)
numpy
pillow
pillow-heif
httpx
httpx2
faiss-cpu          # fast face similarity search; imported as `faiss`
python-dotenv
itsdangerous
pynvml         # GPU name on NVIDIA hardware; optional, graceful fallback
py-cpuinfo     # reliable CPU name cross-platform
psutil         # system memory reporting
